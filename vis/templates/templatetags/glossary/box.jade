.MetaPanel
  h1.MetaPanel-title Glossary
  dl.DefinitionList
    - for item in items
      - with item.glossary_item as item
        dt
          a(name=item.slug)
          #{item.name}
        dd= item.description
  a(href="{% url 'glossary' %}") See the full glossary

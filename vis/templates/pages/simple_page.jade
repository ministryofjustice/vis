- extends 'article.jade'
- load glossary wagtailcore_tags wagtailextra_tags

block article_title
  = self.title

block article_content
  #{self.content|heading_ids|richtext}

block article_aside
  - with self.link_items.all as l_items
    - include "templatetags/links/box.jade"
  - with self.glossary_items.all as items
    - include "templatetags/glossary/box.jade"
